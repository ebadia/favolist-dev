export enum status {
  PENDING = 'PENDING',
  ORDERING = 'ORDERING',
  RECEIVED = 'RECEIVED',
  OPENED = 'OPENED',
  READY = 'READY'
}

export enum roles {
  ADMIN = 'ADMIN',
  BUYER = 'BUYER',
  SELLER = 'SELLER'
}

export enum DayOfWeek {
  monday = 0,
  tuesday = 1,
  wednesday = 2,
  thursday = 3,
  friday = 4,
  saturday = 5,
  sunday = 6
}
