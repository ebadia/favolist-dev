{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "tincfeina",
  "database": "favolist_dev",
  "entities": ["src/**/*.entity{.ts,.js}"],
  "migrations": ["src/**/*.migration{.ts,.js}"],
  "synchronize": true,
  "logging": true,
  "cli": {
    "entitiesDir": "src/entities"
  }

}